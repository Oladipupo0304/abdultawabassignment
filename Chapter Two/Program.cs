
Console.WriteLine("===chapter two===");

Console.WriteLine("Answer to number 1.");
 sbyte noOfDays  = -128;
   byte noOfTime = 254;
   short noOfWeek = 32767;
   ushort noOfMonnth = 65535;
   int noOfYear = 10;
   uint dice = 1007;
   long time = -9223372036854775807;
   ulong week = 12345678909876543215;

Console.WriteLine("Answer to number 2.");

float = 5, -5.01, 34.567839023, 12.345
double = 5, -5.01, 34.567839023, 12.345,
    8923.1234857, a 3456.091124875956542151256683467. 
decimal = 5, -5.01, 34.567839023, 12.345, 8923.1234857,
  3456.091124875956542151256683467.


{
   
    {
        double number1 = 5.123456;
        double number2 = 5.123457;
        double accuracy= 0.000001;

        bool areEqual = Math.Abs(number1 - number2) <= accuracy;

        if (areEqual)
        {
            Console.WriteLine("The two numbers are equal within the specified accuracye.");
        }
        else
        {
            Console.WriteLine("The two numbers are not equal within the specified accuracy.");
        }
    }
}

Console.WriteLine("Answer to number 4");

int hexValue = 0x100;

Console.WriteLine("answer to number  5.");


Console.WriteLine("answer to number 6.");
bool isMale = true; 

Console.WriteLine("you are a male");


Console.WriteLine("answer to number 7.");
string firstString = "Hello";
string secondString = "World";

// Concatenate the two strings with a space and store it in an object variable
object concatenatedStrings = firstString + " " + secondString;

// Print the object variable
Console.WriteLine(concatenatedStrings);

Console.WriteLine("answer to number 8.");
string firstString2 = "Hello";
string secondString2 = "World";

// Concatenate the two string variables with a space and store the result in an object variable
object concatenatedString = firstString + " " + secondString;

// Declare a third string variable and initialize it by casting the object variable
string thirdString = (string)concatenatedStrings;

// Now, thirdString contains the concatenated string "Hello World"

// Print the result
Console.WriteLine(thirdString);

string stringWithQuotes = "The \"use\" of quotations causes difficulties.";
string withoutQuotes = "The use of quotations causes difficulties.";

Console.WriteLine("answer to number 10.");



Console.WriteLine("Answer to number 11")

{
    static void Main()
    {
        int height = 5; // You can change the height as needed

        for (int i = 1; i <= height; i++)
        {
            // Print spaces before the copyright symbols
            for (int j = height - i; j > 0; j--)
            {
                Console.Write(" ");
            }

            // Print the copyright symbols
            for (int k = 1; k <= 2 * i - 1; k++)
            {
                Console.Write("©");
            }

            Console.WriteLine(); // Move to the next line for the next row
        }
    }
}
Console.WriteLine("Answer to numbber 12");
{
    // Attributes
    string FirstName;
    string LastName;
    int Age;
    char Gender; // for male = m  for female ='f' 
    int EmployeeNumber; // Unique employee number (27560000 to 27569999)
    }

Console.WriteLine("answer to number 13.")  ;


    {
        int a = 5;
        int b = 10;

        // Swapping the value
        int temporary = a;
        a = b;
        b = temporary;

        // then you print the swapped value
        Console.WriteLine("a = " + a);
        Console.WriteLine("b = " + b);
    }



